import csv
import os
from datetime import datetime

# Input and output file paths
input_file = "./output/ai_signal/daily_buy.csv"
final_output = "./output/ai_signal/strong_ratio.csv"

# Create output directory if it doesn't exist
os.makedirs(os.path.dirname(final_output), exist_ok=True)

# Read and process data
rows = []

with open(input_file, 'r', newline='', encoding='utf-8') as infile:
    reader = csv.DictReader(infile)
    
    # Check if required columns exist
    if not all(col in reader.fieldnames for col in ['rt', 'bbr', 'strong']):
        raise ValueError("Input file must have rt, bbr, strong columns")
    
    for row in reader:
        # Create new row with only rt, bbr, strong
        new_row = {
            'rt': row['rt'],
            'bbr': row['bbr'],
            'strong': row['strong']
        }
        rows.append(new_row)

# Write to final CSV file
with open(final_output, 'w', newline='', encoding='utf-8') as outfile:
    fieldnames = ['rt', 'bbr', 'strong']
    writer = csv.DictWriter(outfile, fieldnames=fieldnames)
    
    writer.writeheader()
    writer.writerows(rows)

# Console report
print(f"\n{'='*50}")
print(f"Data saved")
print(f"{'='*50}")

for idx, row in enumerate(rows, start=1):
    print(f"Row {idx}: {row['rt']} | BBR={row['bbr']} | Strong={row['strong']}")

print(f"\n{'='*50}")
print(f"File saved: {final_output}")
print(f"Total rows processed: {len(rows)}")
print(f"{'='*50}")
